import AsyncHandler from "../utils/AsyncHandler.ts";

// get all campaigns
export const campaigns = AsyncHandler(async(req,res)=>{
    
})

// upload campaign
export const uploadCampaign = AsyncHandler(async(req,res)=>{

})

// edit campaign
export const editCampaign = AsyncHandler(async(req,res)=>{

})

// delete campaign
export const deleteCampaign = AsyncHandler(async(req,res)=>{

})